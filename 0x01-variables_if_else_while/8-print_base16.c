#include <stdio.h>
/**
 * main- entry point
 *
 * return: always 0 (success)
 */

int main(void}
{
	int i;

	for (i = 0; i < 10; 1++)
{
	putchar(i + '0');
}
	for (i = 0; i < 6; i++)
{
	putchar(1 + 'a');
}
	putchar('\n');

	return (0);
}

